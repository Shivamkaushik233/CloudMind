# Terraform — Phase 8 stub

Will provision the real AWS footprint (EKS, RDS, ElastiCache, DynamoDB,
MSK, S3, CloudFront, IAM) once we're ready to move off local Docker/K8s.
Not applied anywhere yet — no AWS account is wired into this environment.
