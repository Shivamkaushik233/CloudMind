services:
  - type: web
    name: cloudmind
    env: python
    buildCommand: pip install -r backend/core-api/requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT --app-dir backend/core-api
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
