# Anomaly Detection — Phase 5 stub

Will detect abnormal latency / error-rate / CPU patterns against expected
baselines and emit anomaly events onto the (simulated) event bus for the
AI Root-Cause Agent to pick up. Metrics: precision / recall / F1.
