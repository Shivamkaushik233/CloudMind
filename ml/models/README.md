# Trained model artifacts

Serialized models (`.joblib`, `.pt`, etc.) will be saved here once
`ml/forecasting` and `ml/anomaly-detection` are implemented. Gitignored
in a real repo — kept here only as the expected drop location.
