# Traffic & Resource Forecasting — Phase 5 stub

Will contain:
- `xgboost_baseline.py` — structured-metrics baseline forecaster
- `lstm_forecaster.py` — time-series traffic prediction
- `transformer_forecaster.py` — longer-horizon forecasting
- `train.py`, `evaluate.py` (MAE / RMSE / MAPE)

Not implemented yet — see `backend/core-api` for the current working piece
of CloudMind (Phase 1: Cloud Management System).
