# Observability — Phase 4 stub

Local dev plan: Prometheus scraping core-api's `/metrics` endpoint +
Grafana dashboard. `docker-compose.yml` (once added under
`infrastructure/docker`) will wire these up. OpenTelemetry tracing and
CloudWatch are AWS-stage (Phase 8) additions.
